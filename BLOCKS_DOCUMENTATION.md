# 📚 Dokumentasi YOLOv12 Custom Blocks

Dokumentasi lengkap untuk semua custom blocks yang digunakan dalam YOLOv12 untuk small object detection, khususnya untuk BTA/AFB detection.

---

## 📋 Daftar Blocks

### Core Blocks
1. **SmallObjectBlock** - Advanced Small Object Detection Block
2. **RFCBAM** - Receptive Field Channel and Spatial Attention Module

### Small Object Detection Blocks (Baris 56-70)
3. **DPCB** - Detail-Preserve Context Block
4. **BFB** - Balanced Fusion Block
5. **SOFP** - Small Object Feature Pyramid Block
6. **HRDE** - High-Res Detail Extractor Block
7. **MDA** - Multi-Dilation Aggregator Block
8. **DSOB** - Dense Small Object Block
9. **EAE** - Edge-Aware Enhancement Block
10. **CIB2** - Context Injection Block
11. **TEB** - Texture Enhancement Block
12. **FDB** - Feature Decoupling Block
13. **SACB** - Scale-Aware Convolution Block
14. **FBSB** - Foreground-Background Separation Block
15. **FDEB** - Frequency Domain Enhancement Block
16. **DPRB** - Dense Prediction Refinement Block

---

## 🔍 Detail Arsitektur

### 1. SmallObjectBlock

**Tujuan**: Advanced Small Object Detection Block yang dioptimalkan untuk BTA/AFB detection.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Multi-Scale Feature Extraction      │
│  ├─ Conv 1x1 (global context)       │ → c2//4
│  ├─ Conv 3x3 (local features)      │ → c2//2
│  └─ Conv 5x5 (larger context)      │ → c2//4
└─────────────────────────────────────┘
  ↓ (Weighted Fusion dengan learnable weights)
┌─────────────────────────────────────┐
│ Feature Fusion (Conv 1x1)           │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Enhanced Attention                  │
│  ├─ Channel Attention (SE)          │
│  └─ Spatial Attention (7x7)         │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Feature Refinement (Conv 3x3)       │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Residual Connection (optional)      │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Multi-scale feature extraction (1x1, 3x3, 5x5)
- Enhanced CBAM dengan residual connection
- Feature fusion dengan learnable weights
- Spatial detail preservation

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels
- `kernel_size`: Kernel size untuk spatial attention (default: 7)
- `use_residual`: Use residual connection (default: True)

---

### 2. RFCBAM (Receptive Field Channel and Spatial Attention Module)

**Tujuan**: Receptive field attention-based convolution untuk downsampling operations.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Group Convolution 3x3               │
│ (extract receptive field features)  │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ BatchNorm + ReLU                    │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Dimensional Transformation          │
│ Expand by 3x3 = 9x                  │
│ (each spatial location → 9 features)│
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Attention Mechanism                 │
│  ├─ Spatial Attention (7x7)         │
│  │   (mean + max pooling)           │
│  └─ Channel Attention (SE)          │
│      (Squeeze-and-Excitation)       │
└─────────────────────────────────────┘
  ↓ (Element-wise multiplication)
┌─────────────────────────────────────┐
│ Final Convolution 3x3               │
│ (resize/downsample)                 │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Receptive field attention mechanisms
- Mitigates information dilution dari parameter sharing
- Spatial + Channel attention fusion

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels
- `k`: Kernel size (default: 3)
- `s`: Stride (1 for same size, 2 for downsampling)
- `g`: Groups untuk group convolution (default: None, auto-calculate)

---

### 3. DPCB (Detail-Preserve Context Block)

**Tujuan**: Menambahkan context tanpa menghancurkan detail information.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (reduce channel)           │
│ reduction = 0.5                      │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ DWConv 3x3 stride=1                 │
│ (preserve detail)                   │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Dilated DWConv 3x3 (dilation=2)    │
│ (wider context tanpa pooling)       │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (restore channel)          │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Residual Connection (optional)      │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Tidak ada maxpooling (preserves spatial detail)
- Receptive field meningkat via dilation
- Residual connection preserves original features

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels
- `reduction`: Channel reduction factor (default: 0.5)
- `dilation`: Dilation rate (default: 2)
- `use_residual`: Use residual connection (default: True)

---

### 4. BFB (Balanced Fusion Block)

**Tujuan**: Balanced processing untuk high-res dan low-res features di neck architecture.

**Arsitektur**:
```
Input (concatenated high-res + low-res)
  ↓
┌─────────────────────────────────────┐
│ Split Channels                       │
│  ├─ High-res branch (first half)    │
│  └─ Low-res branch (second half)    │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Process Each Branch                 │
│  ├─ Conv 1x1 (high-res)             │
│  └─ Conv 1x1 (low-res)              │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate Both Branches           │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Fusion Blocks (repeat n times)      │
│  ├─ DWConv 3x3                      │
│  └─ Conv 1x1                        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Final Output Projection (Conv 1x1)  │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Balanced processing high-res dan low-res features
- Prevents high-res features dari overwhelmed
- Preserves detail dari high-resolution branch
- Efficient dengan depthwise convolution

**Parameter**:
- `c1`: Input channels (should be 2x output after Concat)
- `c2`: Output channels
- `n`: Number of DWConv3x3 + Conv1x1 repetitions (default: 2)
- `use_residual`: Use residual connection (default: True)

---

### 5. SOFP (Small Object Feature Pyramid Block)

**Tujuan**: Enhanced small object detection dengan feature pyramid.

**Arsitektur**:
```
Input: [neck_output, p2_backbone]
  ↓
┌─────────────────────────────────────┐
│ Process Neck Output                 │
│  ├─ Conv 3x3 stride=1               │
│  └─ Conv 3x3 stride=1               │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Upsample 2x (bilinear)              │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (reduce to P2 channels)   │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate with Backbone P2        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1 (final)           │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Feature pyramid untuk multi-scale detection
- Fusion dengan backbone P2 features
- Upsampling untuk spatial alignment

**Parameter**:
- `c1`: Input channels (from neck)
- `c2`: Output channels
- `c_p2`: Backbone P2 channels (for concatenation)

**Lokasi**: Neck, sebelum P2 detection head

---

### 6. HRDE (High-Res Detail Extractor Block)

**Tujuan**: Preserving fine details di P2 stage.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (reduce channel)           │
│ reduction = 0.5                      │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ DepthwiseConv 5x5 stride=1          │
│ padding=2                            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (expand channel)           │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Residual Add                        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ GELU Activation                     │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1                   │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Depthwise convolution untuk detail preservation
- GELU activation
- Residual connection

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels
- `reduction`: Channel reduction ratio (default: 0.5)

**Lokasi**: Backbone P2 stage, setelah stem, sebelum first C2f

---

### 7. MDA (Multi-Dilation Aggregator Block)

**Tujuan**: Multi-scale feature aggregation dengan different dilations.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (channel reduction)        │
│ reduction_ratio = 4                  │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Parallel Branches                   │
│  ├─ Conv 3x3 dilation=1            │
│  ├─ Conv 3x3 dilation=2            │
│  └─ Conv 3x3 dilation=3            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate All Branches            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (channel reduction)        │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Multi-dilation untuk different receptive fields
- Parallel processing
- Efficient channel reduction

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels
- `reduction_ratio`: Channel reduction ratio (default: 4)

**Lokasi**: Neck, di FPN path antara P3→P2

---

### 8. DSOB (Dense Small Object Block)

**Tujuan**: Dense feature extraction untuk P2 head.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1 → f1              │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1 → f2              │
│ (process f1)                         │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate [input, f1, f2]        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (bottleneck)               │
│ bottleneck_ratio = 0.5              │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1                   │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Dense connections (input → f1 → f2)
- Bottleneck untuk efficiency
- Cascade processing

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels
- `bottleneck_ratio`: Bottleneck channel ratio (default: 0.5)

**Lokasi**: Neck P2 head, replace Conv sequence sebelum final detection layer

---

### 9. EAE (Edge-Aware Enhancement Block)

**Tujuan**: Edge-aware feature enhancement dengan Sobel filters.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Main Branch                         │
│ Conv 3x3 stride=1 → feature_main    │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Edge Detection Branch               │
│  ├─ Sobel X filter (fixed)         │
│  ├─ Sobel Y filter (fixed)         │
│  └─ Gradient magnitude              │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 on edge_map                │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Multiply: feature_main * (1 + edge) │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1                   │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Residual Add                        │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Fixed Sobel filters untuk edge detection
- Edge-enhanced feature multiplication
- Residual connection

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels

**Lokasi**: Backbone P2, inject setelah first C2f

---

### 10. CIB2 (Context Injection Block)

**Tujuan**: Injecting context dari P3 ke P2.

**Arsitektur**:
```
Input: [p2, p3]
  ↓
┌─────────────────────────────────────┐
│ Upsample P3 2x (bilinear)           │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (match P2 channels)       │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate [P2, P3_up]            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1                   │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ MaxPool 3x3 stride=1 padding=1     │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 stride=1                   │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1                           │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Multi-scale context injection
- MaxPool untuk spatial aggregation
- Channel matching

**Parameter**:
- `c_p2`: P2 channels
- `c_p3`: P3 channels
- `c_out`: Output channels (default: same as P2)

**Lokasi**: Neck, di lateral connection P3→P2

---

### 11. TEB (Texture Enhancement Block)

**Tujuan**: Enhancing high-frequency details untuk AFB detection.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Laplacian Filter Branch             │
│  ├─ Laplacian filter (fixed)        │
│  │   [[0, -1, 0],                    │
│  │    [-1, 4, -1],                   │
│  │    [0, -1, 0]]                    │
│  └─ Conv 1x1 on edge features       │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Original Feature Branch             │
│ Conv 1x1                            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate for Weighted Fusion     │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Generate Learnable Weights           │
│ Conv 1x1 → Softmax                  │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Weighted Fusion                     │
│ w_edge * edge + w_original * orig  │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 Projection                 │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Laplacian filter untuk edge detection
- Learnable weighted fusion
- Dual branch processing

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels

**Lokasi**: Backbone P2 stage, setelah downsample pertama

---

### 12. FDB (Feature Decoupling Block)

**Tujuan**: Memisahkan semantic vs localization features.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Dual Pathway                         │
│  ├─ Semantic Branch                 │
│  │   Conv 3x3 dilation=2            │
│  │   (wider receptive field)        │
│  └─ Localization Branch             │
│      Conv 3x3 stride=1              │
│      (local features)                │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Cross-Fusion                         │
│  ├─ semantic_fused =                 │
│  │   semantic + fusion(localization)│
│  └─ localization_fused =            │
│      localization + fusion(semantic) │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate Both Branches           │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 1x1 (final output)             │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Dual pathway untuk semantic dan localization
- Cross-fusion antara branches
- Feature decoupling

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels

**Lokasi**: Neck, khusus di P2 head sebelum bbox regression

---

### 13. SACB (Scale-Aware Convolution Block)

**Tujuan**: Adaptive kernel size berdasarkan feature statistics.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Compute Statistics                   │
│  ├─ Mean per channel                 │
│  └─ Variance per channel             │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Combine Statistics                  │
│ Conv 1x1                            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Generate Kernel Size Weights        │
│ Conv 1x1 → Softmax                  │
│ (3 weights for 3 kernels)            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Multi-Kernel Convolution            │
│  ├─ Conv 3x3                        │
│  ├─ Conv 5x5                        │
│  └─ Conv 7x7                        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Weighted Combination                │
│ w3*out3 + w5*out5 + w7*out7        │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Adaptive kernel size berdasarkan statistics
- Small object → larger kernel
- Multi-kernel dengan learned weights

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels

**Lokasi**: Replace standard Conv di C2f bottleneck P2

---

### 14. FBSB (Foreground-Background Separation Block)

**Tujuan**: Explicit FG/BG modeling.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Generate Attention Mask             │
│ Conv 1x1 → Sigmoid                  │
│ [B, 1, H, W]                        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Foreground Stream                   │
│ features * mask                      │
│ Conv 3x3 → c2//2                    │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Background Stream                    │
│ features * (1-mask)                  │
│ Conv 3x3 → c2//2                    │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate [FG, BG]                │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Final Fusion (Conv 1x1)             │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- Explicit foreground-background separation
- Learnable attention mask
- Dual stream processing

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels

**Lokasi**: Neck setelah P2 upsampling

---

### 15. FDEB (Frequency Domain Enhancement Block)

**Tujuan**: FFT-based high-frequency boost.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ FFT Transform (rfft2)               │
│ [B, C, H, W] → [B, C, H, W//2+1]    │
│ (complex)                            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Separate Real & Imaginary           │
│  ├─ Real part                       │
│  └─ Imaginary part                  │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Concatenate [Real, Imag]             │
│ [B, 2*C, H, W//2+1]                 │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Amplify High-Frequency             │
│ Conv 1x1 (learnable)                │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Split Back to Real & Imaginary      │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Reconstruct Complex Tensor          │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ IFFT Transform (irfft2)             │
│ [B, C, H, W//2+1] → [B, C, H, W]   │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Residual Add dengan Original         │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Final Projection (Conv 1x1)         │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- FFT-based frequency domain processing
- High-frequency amplification
- Residual connection

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels

**Lokasi**: Preprocessing di Backbone P2 input

---

### 16. DPRB (Dense Prediction Refinement Block)

**Tujuan**: Extra dense conv layers untuk P2.

**Arsitektur**:
```
Input
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 → f1                       │
│ (c1 → c_mid)                        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Dense Connection: [input, f1]      │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 → f2                       │
│ (c1 + c_mid → c_mid)                │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Dense Connection: [input, f1, f2]   │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Conv 3x3 → f3                       │
│ (c1 + 2*c_mid → c_mid)              │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Dense Connection:                   │
│ [input, f1, f2, f3]                 │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Bottleneck (Conv 1x1)               │
│ (c1 + 3*c_mid → c2)                 │
└─────────────────────────────────────┘
  ↓
Output
```

**Fitur Kunci**:
- 3 cascade Conv 3x3
- Dense connections (connect all to all)
- Bottleneck di akhir

**Parameter**:
- `c1`: Input channels
- `c2`: Output channels

**Lokasi**: Neck P2, replace detection head convolutions

---

## 📊 Ringkasan Lokasi Blocks

| Block | Lokasi | Tujuan |
|-------|--------|--------|
| **SmallObjectBlock** | Flexible | Multi-scale small object detection |
| **RFCBAM** | Flexible | Receptive field attention downsampling |
| **DPCB** | Flexible | Detail-preserve context |
| **BFB** | Neck | Balanced fusion high/low-res |
| **SOFP** | Neck (before P2 head) | Feature pyramid untuk P2 |
| **HRDE** | Backbone P2 (after stem) | High-res detail extraction |
| **MDA** | Neck (P3→P2 path) | Multi-dilation aggregation |
| **DSOB** | Neck P2 head | Dense feature extraction |
| **EAE** | Backbone P2 (after first C2f) | Edge-aware enhancement |
| **CIB2** | Neck (P3→P2 lateral) | Context injection |
| **TEB** | Backbone P2 (after downsample) | Texture enhancement |
| **FDB** | Neck P2 (before bbox reg) | Feature decoupling |
| **SACB** | Backbone P2 (C2f bottleneck) | Scale-aware convolution |
| **FBSB** | Neck (after P2 upsampling) | FG/BG separation |
| **FDEB** | Backbone P2 (preprocessing) | Frequency domain enhancement |
| **DPRB** | Neck P2 (detection head) | Dense prediction refinement |

---

## 🎯 Use Cases

### Small Object Detection (BTA/AFB)
- **Primary**: SmallObjectBlock, SOFP, HRDE, DSOB, DPRB
- **Supporting**: EAE, TEB, FDEB, SACB

### Multi-Scale Features
- **Primary**: MDA, SOFP, SACB
- **Supporting**: DPCB, BFB

### Context Enhancement
- **Primary**: CIB2, DPCB
- **Supporting**: BFB

### Edge/Texture Enhancement
- **Primary**: EAE, TEB
- **Supporting**: FDEB

### Feature Separation/Decoupling
- **Primary**: FDB, FBSB
- **Supporting**: BFB

---

## 📝 Catatan Implementasi

1. **Multi-Input Blocks**: SOFP dan CIB2 memerlukan 2 input tensors (list)
2. **Fixed Filters**: EAE dan TEB menggunakan fixed Sobel/Laplacian filters
3. **FFT Processing**: FDEB menggunakan FFT/IFFT untuk frequency domain
4. **Dense Connections**: DPRB menggunakan dense connections (all-to-all)
5. **Attention Mechanisms**: RFCBAM, SmallObjectBlock menggunakan attention
6. **Residual Connections**: Kebanyakan blocks support residual connections

---

## 🔧 Tips Penggunaan

1. **Untuk Small Objects**: Gunakan kombinasi SOFP + HRDE + DSOB
2. **Untuk Multi-Scale**: Gunakan MDA + SACB
3. **Untuk Edge Detection**: Gunakan EAE + TEB
4. **Untuk Context**: Gunakan CIB2 + DPCB
5. **Untuk Refinement**: Gunakan DPRB di detection head

---

**Dokumentasi ini dibuat untuk YOLOv12 Custom Blocks v1.0**

